# calculating the Area of triangle (formula - (height*base)/2 )

height = float(input('enter the height of the trianlge: '))
base = float(input('enter the base of the triangle: '))

area = (0.5)*base*height
print(f'the area of trianlg is {area}')